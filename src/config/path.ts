import * as path from "path";

export const BASE_DIR = path.resolve(__dirname, "../../");
export const DEFAULT_OUTPUT_FILE = path.join(BASE_DIR, "./output/data/output.json");
export const DEFAULT_INPUT_TEST_DIR = path.join(BASE_DIR, "./input/data");
