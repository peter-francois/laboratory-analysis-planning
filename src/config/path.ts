import * as path from "path";

export const BASE_DIR = path.resolve(__dirname, "../../");
export const DEFAULT_OUTPUT_DIR = path.join(BASE_DIR, "./output");
export const DEFAULT_INPUT_TEST_DIR = path.join(BASE_DIR, "./tests/input/");
